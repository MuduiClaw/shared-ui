# shared-ui — CLAUDE.md

@AGENTS.md

本文件是 Claude Code 在本项目的入口投影，不维护项目规则真源，也不复制项目治理全文。

## 投影规则

- 绝对前提：必须消费上方 `@AGENTS.md` 投影，再做任何项目判断、计划、修改、验证或交付。
- 项目规则真源：`./AGENTS.md`
- Claude Code 进入本项目时按 `AGENTS.md` 的索引读取项目文档和合同面。
- 不从本文件推断项目背景、技术栈、门禁、禁令、发布口径或长期教训。
- 可用 Subagent 加速调查、独立审查和边界明确的实现分片；提交、推送、发布、回滚、生产动作和交付回执仍由主会话按项目规则收口。
- 需要每次必然执行的约束时，用项目 `.claude/settings.json` hooks 或项目脚本做物理阻断，不能只写在本文件里。

## 更新规则

- 需要新增、删除或修改项目规则时，修改 `AGENTS.md`。
- 如果认为 `CLAUDE.md` 也需要更新，只能同步本投影说明；项目事实和规则仍写入 `AGENTS.md`。
- 不在本文件记录状态、验收结论、发布结果、临时任务或事故复盘。
